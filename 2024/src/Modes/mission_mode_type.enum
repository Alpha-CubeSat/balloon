#ifndef mission_mode_type_enum_
#define mission_mode_type_enum_

enum class mission_mode_type: unsigned char { //unsigned char is a type that can only take values from 0 to 255
    deployment,
    high_altitude,
    standby,
    post_deployment};

#endif